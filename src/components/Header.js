import React from 'react';

import Notifications from '~/components/Notifications';

const Header = () => {
  return (
    <header>
      <Notifications />
    </header>
  );
};

export default Header;
