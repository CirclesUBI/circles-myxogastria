import PropTypes from 'prop-types';
import React, { Fragment } from 'react';

const Send = () => {
  return <Fragment />;
};

Send.contextTypes = {
  t: PropTypes.func.isRequired,
};

export default Send;
